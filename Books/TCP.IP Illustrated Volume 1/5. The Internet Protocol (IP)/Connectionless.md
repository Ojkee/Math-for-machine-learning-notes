It means that it does not maintain any connection state information about related [[Datagram|datagrams]] within the network elements (i.e., within the routers). Each datagram is handled independently from all other others. That implies that packets could go different paths and arrive in different order.

